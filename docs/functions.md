# Functions

```universal
FUNCTION double(value: Number) -> Number
    RETURN value * 2
END
```

V0.1 supports positional function arguments and named arguments for entity construction. Function overloads, generics, closures, and async functions are future work.
